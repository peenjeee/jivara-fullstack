{
  "$schema": "https://railway.app/sm_config_schema.json",
  "build": {
    "builder": "NIXPACKS",
    "rootDirectory": "backend",
    "buildCommand": "npm run build"
  },
  "deploy": {
    "startCommand": "npm run start",
    "healthCheckPath": "/",
    "healthCheckTimeout": 100,
    "restartPolicyType": "ON_FAILURE"
  }
}
